# Icons

The renderer currently loads icons from the installed `lucide-static` package.
Change the `ICON_TO_LUCIDE` mapping in `src/keycaps.js` to select another Lucide
icon. Files placed in this directory do not automatically override that mapping.
